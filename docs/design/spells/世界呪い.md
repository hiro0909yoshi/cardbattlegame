# 世界刻印システム

**最終更新**: 2025年12月2日 | **実装状況**: 🔶 設計中

---

## 概要

世界刻印は、ゲーム全体に影響を与える特殊な刻印効果。
- **継続期間**: 6ラウンド（全て共通）
- **上書きルール**: 新しい世界刻印で上書き（同時に1つのみ有効）
- **保存場所**: `game_flow_manager.game_stats["world_curse"]`

---

## タスク管理

### 実装状況

| ID | 名前 | 効果 | 難易度 | 状況 |
|----|------|------|--------|------|
| 2009 | ライズオブサン | カード使用EP：袋1.5倍、冠袋2倍 | 中 | ✅ 完了 |
| 2036 | ボンドオブラバーズ | 火と地、水と風が連鎖 | 高 | ⬜ 未着手 |
| 2047 | インペリアルガード | 全ドミニオは領土守護 | 中 | ✅ 完了 |
| 2048 | ハイプリーステス | 刻印付きクリーチャーは結界 | 低 | ✅ 完了 |
| 2064 | ハングドマンズシール | アルカナアーツ、自破壊時、戦闘終了子時は発動しない | 高 | ⬜ 未着手 |
| 2081 | フールズフリーダム | クリーチャー配置時、EP以外のコスト無視 | 低 もう実装はしてるんで1階確認してもらえますか？ただねちょっと想定と動くが違うんで修正したいんですよ。| ✅ 完了（効果は召喚条件実装後） |
| 2102 | テンパランスロウ | 順位が下のセプターのドミニオに休戦 | 中 | ✅ 完了 |
| 2110 | エンプレスドメイン | 全セプターはスペルの対象に選べない | 低 | ✅ 完了 |
| 2111 | ハーミットズパラドックス | 同クリーチャー2体配置なら戦闘時破壊 | 高 | ⬜ 未着手 |

**凡例**: ⬜ 未着手 / 🔶 実装中 / ✅ 完了

---

## データ構造

### 保存形式

```gdscript
game_flow_manager.game_stats["world_curse"] = {
	"curse_type": "cost_increase",   # 刻印タイプ識別子
	"name": "太陽",             # 表示名
	"duration": 6,                   # 残りラウンド数
	"params": {                      # 効果固有パラメータ
		"bag_multiplier": 1.5,
		"crown_bag_multiplier": 2.0
	}
}
```

### curse_type 一覧

| curse_type | スペル | 効果概要 |
|------------|--------|---------|
| `cost_increase` | ライズオブサン | カード使用コスト増加 |
| `element_chain` | ボンドオブラバーズ | 異属性連鎖 |
| `land_protect` | インペリアルガード | 不変 |
| `cursed_antimagic` | ハイプリーステス | 刻印付きに結界付与 |
| `skill_disable` | ハングドマンズシール | 特定スキル無効 |
| `summon_cost_free` | フールズフリーダム | 召喚条件無視 |
| `invasion_restrict` | テンパランスロウ | 節制 |
| `spell_target_immune` | エンプレスドメイン | スペル対象不可 |
| `same_creature_destroy` | ハーミットズパラドックス | 隠者 |

---

## 各スペルの詳細仕様

### 2009: ライズオブサン（太陽）

**効果**: カード使用EPが袋1.5倍、冠袋2倍

**フック場所**: カード使用時のコスト計算
- `spell_phase_handler.gd` - スペル使用コスト
- `dominio_order_handler.gd` - クリーチャー配置コスト
- 戦闘時アイテム使用コスト

**params**:
```gdscript
{
	"bag_multiplier": 1.5,      # 通常カード
	"crown_bag_multiplier": 2.0 # 冠袋カード
}
```

---

### 2036: ボンドオブラバーズ（恋人）

**効果**: 火と地、水と風が連鎖ボーナスを共有

**フック場所**: 連鎖計算
- `board_system_3d.gd` - `calculate_chain_bonus()`

**params**:
```gdscript
{
	"chain_pairs": [
		["fire", "earth"],
		["water", "air"]
	]
}
```

---

### 2047: インペリアルガード（不変）

**効果**: 全ドミニオは領土守護

**フック場所**: 土地操作スペル
- `spell_land_new.gd` - 属性変更・レベル変更メソッド

**params**: なし（フラグのみ）

---

### 2048: ハイプリーステス（刻印結界）

**効果**: 刻印のついた全クリーチャーは結界を得る

**フック場所**: スペルターゲット選択時
- `target_selection_helper.gd` - クリーチャー対象判定

**params**: なし（フラグのみ）

---

### 2064: ハングドマンズシール（吊人）

**効果**: アルカナアーツ、自破壊時、戦闘終了子時は発動しない

**フック場所**: 
- `spell_phase_handler.gd` - アルカナアーツ使用判定
- `battle_system.gd` - スキル発動判定

**params**:
```gdscript
{
	"disabled_triggers": ["mystic_arts", "on_self_destroy", "on_battle_end"]
}
```

---

### 2081: フールズフリーダム（愚者）

**効果**: クリーチャー配置時、EP以外のコストは無視

**フック場所**: クリーチャー配置判定
- `dominio_order_handler.gd` - 配置条件チェック

**params**: なし（フラグのみ）

---

### 2102: テンパランスロウ（節制）

**効果**: 順位が下のセプターのドミニオに侵略できない

**フック場所**: 侵略可否判定
- `game_flow_manager.gd` - 侵略選択時

**params**: なし（フラグのみ）

---

### 2110: エンプレスドメイン（結界）

**効果**: 全セプターはスペルの対象に選べない

**フック場所**: スペルターゲット選択時
- `target_selection_helper.gd` - プレイヤー対象判定
- `spell_phase_handler.gd` - 対象選択UI

**params**: なし（フラグのみ）

---

### 2111: ハーミットズパラドックス（隠者）

**効果**: 同クリーチャー2体配置なら戦闘時破壊

**フック場所**: 戦闘開始時
- `battle_system.gd` - 戦闘開始処理

**params**: なし（フラグのみ）

---

## 実装ファイル

| ファイル | 役割 |
|---------|------|
| `scripts/spells/spell_curse.gd` | 世界刻印管理（`curse_world()`, `get_world_curse()`） |
| `scripts/spells/spell_world_curse.gd` | 世界刻印効果判定（新規作成） |
| `scripts/spells/spell_protection.gd` | 結界判定（既存・世界刻印対応済み） |
| `scripts/game_flow/spell_phase_handler.gd` | スペル使用時の世界刻印適用 |

---

## SpellWorldCurseクラス設計

### 概要

世界刻印の判定・付与を1クラスに集約。既存システム（SpellProtection等）と連携。

### クラス構造

```gdscript
class_name SpellWorldCurse

# ========================================
# 参照
# ========================================
var spell_curse: SpellCurse

func setup(curse: SpellCurse):
	spell_curse = curse

# ========================================
# 刻印付与（エントリポイント）
# ========================================

## effect辞書から世界刻印を適用
func apply(effect: Dictionary) -> void:
	var curse_type = effect.get("curse_type", "")
	var params = effect.get("params", {})
	var name = effect.get("name", "")
	params["name"] = name
	spell_curse.curse_world(curse_type, 6, params)

# ========================================
# 判定メソッド（static）
# ========================================

## インペリアルガード: 属性変化が無効か
static func is_land_change_blocked(game_stats: Dictionary) -> bool:
	var world_curse = game_stats.get("world_curse", {})
	return world_curse.get("curse_type") == "land_protect"

## テンパランスロウ: 下位侵略が制限されているか
static func is_invasion_restricted(attacker_rank: int, defender_rank: int, game_stats: Dictionary) -> bool:
	var world_curse = game_stats.get("world_curse", {})
	if world_curse.get("curse_type") != "invasion_restrict":
		return false
	# attacker_rank < defender_rank = 攻撃者が上位 → 制限
	return attacker_rank < defender_rank

## ライズオブサン: コスト倍率を取得
static func get_cost_multiplier(card: Dictionary, game_stats: Dictionary) -> float:
	var world_curse = game_stats.get("world_curse", {})
	if world_curse.get("curse_type") != "cost_increase":
		return 1.0
	var params = world_curse.get("params", {})
	# 冠袋カード判定（要実装）
	var is_crown_bag = card.get("is_crown_bag", false)
	if is_crown_bag:
		return params.get("crown_bag_multiplier", 2.0)
	return params.get("bag_multiplier", 1.5)

## フールズフリーダム: 召喚条件が無視されるか
static func is_summon_condition_ignored(game_stats: Dictionary) -> bool:
	var world_curse = game_stats.get("world_curse", {})
	return world_curse.get("curse_type") == "summon_cost_free"

## ハングドマンズシール: 特定トリガーが無効か
static func is_trigger_disabled(trigger_type: String, game_stats: Dictionary) -> bool:
	var world_curse = game_stats.get("world_curse", {})
	if world_curse.get("curse_type") != "skill_disable":
		return false
	var params = world_curse.get("params", {})
	var disabled = params.get("disabled_triggers", [])
	return trigger_type in disabled

## ボンドオブラバーズ: 連鎖ペアを取得
static func get_chain_pairs(game_stats: Dictionary) -> Array:
	var world_curse = game_stats.get("world_curse", {})
	if world_curse.get("curse_type") != "element_chain":
		return []
	var params = world_curse.get("params", {})
	return params.get("chain_pairs", [])

## ハーミットズパラドックス: 隠者が有効か
static func is_same_creature_destroy_active(game_stats: Dictionary) -> bool:
	var world_curse = game_stats.get("world_curse", {})
	return world_curse.get("curse_type") == "same_creature_destroy"

## ハイプリーステス/エンプレスドメイン: SpellProtectionに委譲
# → SpellProtection.is_creature_protected() / is_player_protected() で対応済み
#    world_curse.curse_type == "cursed_protection" / "world_spell_protection"
```

### 既存システムとの連携

| 世界刻印 | 判定場所 | 連携先 |
|---------|---------|--------|
| ハイプリーステス | クリーチャー対象選択時 | `SpellProtection.is_creature_protected()` |
| エンプレスドメイン | プレイヤー対象選択時 | `SpellProtection.is_player_protected()` |
| その他 | 各システム | `SpellWorldCurse.is_xxx()` を直接呼び出し |

### フック追加が必要な箇所

| 世界刻印 | フック場所 | メソッド |
|---------|-----------|---------|
| ライズオブサン | カード使用コスト計算 | `get_cost_multiplier()` |
| インペリアルガード | 土地操作スペル | `is_land_change_blocked()` |
| フールズフリーダム | クリーチャー配置判定 | `is_summon_condition_ignored()` |
| テンパランスロウ | 侵略可否判定 | `is_invasion_restricted()` |
| ハングドマンズシール | スキル発動判定 | `is_trigger_disabled()` |
| ボンドオブラバーズ | 連鎖計算 | `get_chain_pairs()` |
| ハーミットズパラドックス | 戦闘開始時 | `is_same_creature_destroy_active()` |

---

## 実装の流れ

### 1. 刻印付与

```gdscript
# spell_phase_handler.gd
"world_curse":
	var curse_type = effect.get("curse_type", "")
	var params = effect.get("params", {})
	spell_curse.curse_world(curse_type, 6, params)
```

### 2. 刻印判定（各システムでのフック）

```gdscript
# 例: インペリアルガードのチェック
func can_change_land_element(tile_index: int) -> bool:
	var world_curse = spell_curse.get_world_curse()
	if world_curse.get("curse_type") == "land_protect":
		return false  # 不変
	return true
```

### 3. ターン経過

`SpellCurse._update_world_curse()` で duration をカウントダウン

---

## 変更履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| 2025/12/02 | 1.0 | 初版作成 - 設計ドキュメント |

---

**最終更新**: 2025年12月2日 | **実装状況**: 🔶 設計中
