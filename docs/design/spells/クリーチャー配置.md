# クリーチャー配置システム

**バージョン**: 1.1  
**最終更新**: 2025年11月30日  
**ステータス**: 設計フェーズ

---

## 概要

マップ上の空き地にクリーチャーを配置するスペル・秘術のシステム。配置方式は以下の4パターン。

- **指定配置**: 使用者が配置場所を選択（グーバクイーン、コンジャラー秘術）
- **ランダム配置**: ゲームが自動的にランダムな空き地を選択（スパルトイ、マイコロン秘術）
- **条件付き配置**: 条件を満たした場合に配置（ゴブリンズレア）
- **移動時配置**: クリーチャー移動時に複製配置（バウダーイーター）

---

## 実装済みスペル・秘術

### スペル

| ID | 名前 | 配置対象 | creature_id | 配置方式 | ステータス |
|----|------|---------|-----------|--------|----------|
| 2028 | ゴブリンズレア | ゴブリン | 414 | 条件付き（空地停止時） | 未実装 |
| 2043 | スパルトイ | スケルトン | 420 | ランダム | 未実装 |
| 2132 | ワイルドセンス | ドロー済み | 動的 | ランダム | 未実装 |

### 秘術（Phase 3実装対象）

| ID | クリーチャー | 配置対象 | creature_id | 配置方式 | ステータス |
|----|-----------|---------|-----------|--------|----------|
| 12 | コンジャラー | バーアル | 27 | 指定配置 | 未実装 |
| 209 | グーバクイーン | グーバ | 208 | 指定配置 | 未実装 |
| 444 | レジェンドファロス | スタチュー | 421 | 隣接空地 | 未実装 |

---

## effect_type定義

### スペル用

```json
{
  "effect_type": "place_creature",
  "creature_id": 1,
  "placement_mode": "random|select|conditional",
  "condition": {
    "condition_type": "on_empty_land",
    "value": 0
  }
}
```

### 秘術用（直接effects方式）

秘術は `mystic_arts` 配列内に直接 `effects` を記述。クリーチャー配置は秘術専用の効果として実装。

```json
{
  "mystic_arts": [
    {
      "name": "クリーチャー配置",
      "cost": 50,
      "target_type": "land",
      "target_info": {
        "owner_filter": "any",
        "target_filter": "empty"
      },
      "effects": [
        {
          "effect_type": "place_creature",
          "creature_id": 209,
          "placement_mode": "select"
        }
      ]
    }
  ]
}
```

#### placement_mode 一覧

| モード | 説明 | 実装対象 |
|--------|------|--------|
| `select` | プレイヤーが配置場所を選択 | グーバクイーン、コンジャラー秘術 |
| `random` | ゲームがランダムに空き地を選択 | スパルトイ、マイコロン秘術 |
| `adjacent` | 現在地の隣接空き地に配置 | レジェンドファロス秘術 |
| `conditional` | 条件チェック後に配置 | ゴブリンズレア |
| `move_duplicate` | 移動時に移動元・移動先に複製 | バウダーイーター |

#### trigger 一覧（秘術発動タイミング）

| トリガー | 説明 | 対象 |
|---------|------|------|
| `on_mystic_cast` | 秘術発動時（ターゲット選択後） | グーバクイーン、コンジャラー |
| `on_draw` | カードドロー完了後 | ワイルドセンス |

**注**: triggerはSpellMysticArtsで管理。秘術JSONには通常記述しない（発動タイミングは既に定義済み）

**自動複製系（スキルで実装）**:
- `on_enemy_attack_success` - マイコロン（攻撃成功時複製）→ BattleExecution.gd で管理
- `on_creature_move` - バウダーイーター（移動時複製）→ BoardSystem3D.gd で管理

---

## JSON定義

### スペル例

#### 2028: ゴブリンズレア

```json
{
  "id": 2028,
  "name": "ゴブリンズレア",
  "effect_parsed": {
    "target_type": "self",
    "target_filter": "self",
    "condition": {
      "condition_type": "on_empty_land"
    },
    "effects": [
      {
        "effect_type": "place_creature",
        "creature_id": 414,
        "placement_mode": "conditional",
        "condition": {
          "condition_type": "on_empty_land"
        }
      }
    ],
    "return_to_deck": true
  }
}
```

#### 2043: スパルトイ

```json
{
  "id": 2043,
  "name": "スパルトイ",
  "effect_parsed": {
    "target_type": "none",
    "effects": [
      {
        "effect_type": "place_creature",
        "creature_id": 420,
        "placement_mode": "random"
      }
    ]
  }
}
```

#### 2132: ワイルドセンス

```json
{
  "id": 2132,
  "name": "ワイルドセンス",
  "effect_parsed": {
    "target_type": "self",
    "effects": [
      {
        "effect_type": "draw_cards",
        "count": 1
      },
      {
        "effect_type": "place_creature_on_draw",
        "placement_mode": "random"
      }
    ]
  }
}
```

### 秘術例

#### 12: コンジャラー秘術

```json
{
  "id": 12,
  "name": "コンジャラー",
  "ability_parsed": {
    "keywords": ["巻物強打", "秘術"],
    "mystic_arts": [
      {
        "name": "バーアル召喚",
        "cost": 80,
        "target_type": "land",
        "target_info": {
          "owner_filter": "any",
          "target_filter": "empty"
        },
        "effects": [
          {
            "effect_type": "place_creature",
            "creature_id": 27,
            "placement_mode": "select"
          }
        ]
      }
    ]
  }
}
```

**注**: `self_down` はSpellMysticArts内で自動処理（秘術発動後のクリーチャーダウン）

#### 209: グーバクイーン秘術

```json
{
  "id": 209,
  "name": "グーバクイーン",
  "ability_parsed": {
    "keywords": ["秘術"],
    "mystic_arts": [
      {
        "name": "グーバ配置",
        "cost": 20,
        "target_type": "land",
        "target_info": {
          "owner_filter": "any",
          "target_filter": "empty"
        },
        "effects": [
          {
            "effect_type": "place_creature",
            "creature_id": 208,
            "placement_mode": "select"
          }
        ]
      }
    ]
  }
}
```

#### 444: レジェンドファロス秘術

```json
{
  "id": 444,
  "name": "レジェンドファロス",
  "ability_parsed": {
    "keywords": ["秘術"],
    "mystic_arts": [
      {
        "name": "スタチュー配置",
        "cost": 80,
        "target_type": "land",
        "target_info": {
          "owner_filter": "own",
          "target_filter": "empty"
        },
        "effects": [
          {
            "effect_type": "place_creature",
            "creature_id": 421,
            "placement_mode": "adjacent"
          }
        ]
      }
    ]
  }
}
```

**注**: `adjacent` は選択したタイルの隣接空き地全てに配置（複数タイル配置）

---

## 処理フロー

### 指定配置（グーバクイーン、コンジャラー）

```
秘術発動
  ├─ ターゲット選択UI表示
  │  └─ 空き地のみ選択可能
  ├─ クリーチャーID取得
  ├─ CreatureManager.add_creature()で配置
  └─ 魔力消費 + ダウン状態設定
```

### ランダム配置（スパルトイ）

```
スペル発動
  ├─ BoardSystem3D.get_empty_tiles()で空き地取得
  ├─ 空き地がない場合は効果なし
  ├─ ランダムに1つ選択
  └─ CreatureManager.add_creature()で配置
```

### 条件付き配置（ゴブリンズレア）

```
スペル発動
  ├─ 使用者が空き地に停止しているか判定
  ├─ YES → クリーチャー配置 + デッキ復帰スキップ
  └─ NO → 効果なし + デッキ復帰
```

---

## 関連ファイル

| ファイル | 役割 |
|---------|------|
| `scripts/spells/spell_creature_place.gd` | 配置効果の実装（新規作成） |
| `scripts/game_flow/spell_phase_handler.gd` | スペルフェーズでの呼び出し |
| `scripts/spells/spell_mystic_arts.gd` | 秘術での呼び出し |
| `scripts/managers/creature_manager.gd` | クリーチャー配置・管理 |
| `scripts/game_flow/board_system_3d.gd` | 空き地判定・取得、移動時複製（※スキルシステム） |
| `scripts/battle/battle_execution.gd` | 攻撃成功時複製（※スキルシステム） |
| `data/spell_1.json, spell_2.json` | スペル定義 |
| `data/fire_1.json` | コンジャラー定義 |
| `data/earth_1.json` | グーバクイーン定義 |
| `data/neutral_1.json` | レジェンドファロス定義 |

---

## 実装計画

### Phase 1: 基盤クラス作成
- [ ] `SpellCreaturePlace` クラス作成
- [ ] 基本メソッド実装
  - [ ] `place_creature_random()` - ランダム配置
  - [ ] `place_creature_select()` - 指定配置（UIあり）
  - [ ] `place_creature_move_duplicate()` - 移動時複製
  - [ ] `get_valid_placement_tiles()` - 配置可能タイル取得
  - [ ] `validate_placement()` - 配置前チェック

### Phase 2: スペル実装
- [ ] 2043: スパルトイ（ランダム配置、最もシンプル）
- [ ] 2028: ゴブリンズレア（条件チェック + 復帰[ブック]）
- [ ] 2132: ワイルドセンス（ドロー結果による配置）

### Phase 3: 秘術実装
- [ ] ID 12: コンジャラー秘術（指定配置、バーテル）
- [ ] ID 209: グーバクイーン秘術（指定配置、グーバ）
- [ ] ID 444: レジェンドファロス秘術（隣接空地配置）

### Phase 4: 統合テスト
- [ ] SpellPhaseHandler統合
- [ ] SpellMysticArts統合
- [ ] UI表示テスト（ターゲット選択）

### Phase 5: スキルシステムで実装（検討中）
- [ ] ID 335: バウダーイーター（移動時複製）- BoardSystem3D側で管理
- [ ] ID 140: マイコロン（攻撃成功時複製）- BattleExecution.gd側で管理

---

## 変更履歴

| 日付 | バージョン | 内容 |
|------|-----------|------|
| 2025/11/30 | 1.0 | 初版作成、スペル/秘術一覧、effect_type定義、JSON例、実装計画 |
| 2025/11/30 | 1.1 | コンジャラー（ID 12）、バウダーイーター（ID 335）を追加 |
| 2025/12/01 | 1.2 | 🔧 effect_type整理 - 秘術は直接effects方式に統一、placement_mode "specific"→"select"に統一 |
| 2025/12/01 | 1.3 | 🔧 実装範囲明確化 - マイコロン・バウダーイーター（スキルシステム）を Phase 5 に移動、Phase 3 は秘術3種のみ |
| 2025/12/01 | 1.4 | 🔧 creature_id修正 - バーテル→バーアル (27)、各creature_idを実データから確認して修正 |
