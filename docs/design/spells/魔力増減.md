# 魔力増減効果

**バージョン**: 1.0  
**最終更新**: 2025年11月3日

---

## 概要

魔力操作処理の汎用化モジュール。バトル内外で魔力の増減・奪取を扱う。

**実装ファイル**: `scripts/spells/spell_magic.gd`

**初期化**:
```gdscript
# GameFlowManager.setup_systems()
spell_magic = SpellMagic.new()
spell_magic.setup(player_system)
```

---

## メソッド一覧

### add_magic(player_id: int, amount: int) -> void

魔力を増やす。

**引数**:
- `player_id`: プレイヤーID（0-3）
- `amount`: 増加量（0以下の場合は何もしない）

**使用例**:
```gdscript
spell_magic.add_magic(player_id, 200)  # 200G獲得
```

**出力**:
```
[魔力増加] プレイヤー1 +200G → 合計:3200G
```

---

### reduce_magic(player_id: int, amount: int) -> void

魔力を減らす（0未満にはならない）。

**引数**:
- `player_id`: プレイヤーID（0-3）
- `amount`: 減少量（0以下の場合は何もしない）

**使用例**:
```gdscript
spell_magic.reduce_magic(player_id, 100)  # 100G減少
```

**出力**:
```
[魔力減少] プレイヤー1 -100G → 合計:2900G
```

---

### steal_magic(from_player_id: int, to_player_id: int, amount: int) -> int

魔力を奪う（相手から自分へ）。

**引数**:
- `from_player_id`: 奪われるプレイヤーID
- `to_player_id`: 奪うプレイヤーID
- `amount`: 奪取量

**戻り値**:
- 実際に奪った量（相手の魔力が足りない場合は少なくなる）

**使用例**:
```gdscript
var stolen = spell_magic.steal_magic(enemy_id, player_id, 300)
```

**出力**:
```
[魔力奪取] プレイヤー2 -300G → プレイヤー1 +300G
```

---

## 実装アイテム

### 1. ゼラチンアーマー（ID: 1029）

**効果**: HP+40；魔力獲得[受けたダメージ×G5]

**タイミング**: **ダメージを受けた直後**

**JSON定義**:
```json
{
  "stat_bonus": {"hp": 40},
  "effects": [
    {
      "effect_type": "magic_from_damage",
      "trigger": "after_battle",
      "multiplier": 5
    }
  ]
}
```

**実装場所**: `battle_participant.gd - _trigger_magic_from_damage()`

**発動フロー**:
```
ダメージ処理（take_damage）
  → ダメージ計算完了
  → _trigger_magic_from_damage(damage)
  → spell_magic.add_magic(player_id, damage × 5)
```

**実装コード**:
```gdscript
# BattleParticipant.take_damage()
func take_damage(damage: int) -> Dictionary:
	# ... ダメージ処理 ...
	update_current_hp()
	
	# 💰 魔力獲得処理（ゼラチンアーマー）
	_trigger_magic_from_damage(damage)
	
	return damage_breakdown

func _trigger_magic_from_damage(damage: int) -> void:
	if not spell_magic_ref or damage <= 0:
		return
	
	var items = creature_data.get("items", [])
	for item in items:
		for effect in item.get("effect_parsed", {}).get("effects", []):
			if effect.get("effect_type") == "magic_from_damage":
				var multiplier = effect.get("multiplier", 5)
				var amount = damage * multiplier
				spell_magic_ref.add_magic(player_id, amount)
```

**動作例**:
```
キメラ AP:30 → フェイ（ゼラチンアーマー装備）
  ダメージ: 30
  【魔力獲得(ダメージ)】フェイのゼラチンアーマー 
    → プレイヤー1が150G獲得（ダメージ30×5）
```

---

### 2. ゴールドハンマー（ID: 1012）

**効果**: ST+40；攻撃で敵非破壊時、魔力獲得[G200]

**タイミング**: **バトル結果確定直後**

**JSON定義**:
```json
{
  "stat_bonus": {"st": 40},
  "effects": [
    {
      "effect_type": "magic_on_enemy_survive",
      "trigger": "after_battle",
      "amount": 200,
      "condition": "attacker_win_enemy_alive"
    }
  ]
}
```

**実装場所**: `battle_system.gd - _apply_magic_on_enemy_survive()`

**発動フロー**:
```
バトル結果確定（resolve_battle_result）
  ↓
_apply_post_battle_effects()
  ↓
_apply_magic_on_enemy_survive(result, attacker, defender)
  ↓
条件チェック: 攻撃側勝利 & 敵生存
  ↓
spell_magic.add_magic(player_id, 200)
```

**実装コード**:
```gdscript
# BattleSystem._apply_post_battle_effects()
func _apply_post_battle_effects(...):
	# 再生スキル処理
	battle_special_effects.apply_regeneration(attacker)
	battle_special_effects.apply_regeneration(defender)
	
	# 💰 魔力獲得処理（ゴールドハンマー）
	_apply_magic_on_enemy_survive(result, attacker, defender)
	
	match result:
		# ...

func _apply_magic_on_enemy_survive(result, attacker, defender):
	if not spell_magic:
		return
	
	# 攻撃側勝利 & 敵生存の場合
	if result == BattleResult.ATTACKER_SURVIVED:
		_check_and_apply_magic_on_enemy_survive(attacker, defender)
```

**条件**:
- `result == BattleResult.ATTACKER_SURVIVED`（攻撃側勝利 & 敵生存）
- 勝者が攻撃側（`winner.is_attacker == true`）
- 敗者が生存（`loser.is_alive() == true`）

**動作例**:
```
グラディエーター（ゴールドハンマー装備） vs アモン
  結果: 侵略失敗（攻撃側生き残り）
  【魔力獲得(敵非破壊)】グラディエーターのゴールドハンマー
    → プレイヤー1が200G獲得
```

---

### 3. ゴールドグース（ID: 1011）

**効果**: 遺産[MHP×G7]

**タイミング**: **死亡時**

**JSON定義**:
```json
{
  "effects": [
    {
      "effect_type": "legacy_magic",
      "trigger": "on_death",
      "multiplier": 7
    }
  ]
}
```

**実装場所**: `battle_special_effects.gd - check_on_death_effects()`

**発動フロー**:
```
クリーチャー撃破
  ↓
check_on_death_effects(defeated, opponent)
  ↓
アイテムの on_death トリガーをチェック
  ↓
legacy_magic 効果発動
  ↓
MHP = 元のカードデータ.hp + base_up_hp
  ↓
spell_magic.add_magic(player_id, MHP × 7)
```

**実装コード**:
```gdscript
# BattleSpecialEffects.check_on_death_effects()
"legacy_magic":
	if spell_magic_ref:
		var multiplier = effect.get("multiplier", 7)
		var player_id = defeated.player_id
		
		# 死亡後はget_max_hp()がマイナスになる可能性があるため、
		# 元のカードデータからMHPを計算
		var base_hp = defeated.creature_data.get("hp", 0)
		var base_up_hp = defeated.creature_data.get("base_up_hp", 0)
		var mhp = base_hp + base_up_hp
		
		var amount = mhp * multiplier
		spell_magic_ref.add_magic(player_id, amount)
```

**重要な注意点**:
- 死亡後に`get_max_hp()`を呼ぶと、`base_hp`がマイナスになっている可能性がある
- 必ず元のカードデータから`hp + base_up_hp`で計算する

**動作例**:
```
アモン（ゴールドグース装備、MHP=30）撃破
  【遺産発動】アモンのゴールドグース
    → プレイヤー1が210G獲得（MHP30×7）
```

---

## システム統合

### BattleParticipantへの参照設定

`BattleParticipant`に`spell_magic_ref`を設定する必要がある。

**設定場所**: `BattlePreparation.prepare_participants()`

```gdscript
var attacker = BattleParticipant.new(...)
attacker.spell_magic_ref = spell_magic_ref

var defender = BattleParticipant.new(...)
defender.spell_magic_ref = spell_magic_ref
```

**初期化順序** (重要):
```gdscript
# BattleSystem.setup_systems()

# 1. SpellMagicの参照を先に取得
if game_flow_manager_ref:
	if game_flow_manager_ref.spell_magic:
		spell_magic = game_flow_manager_ref.spell_magic

# 2. その後、サブシステムに渡す
battle_preparation.setup_systems(board_system, card_system, player_system, spell_magic)
battle_special_effects.setup_systems(board_system, spell_draw, spell_magic)
```

---

## 発動タイミング一覧

| 効果タイプ | タイミング | 実装場所 | トリガー |
|-----------|----------|---------|---------|
| `magic_from_damage` | ダメージ受け取り直後 | `BattleParticipant.take_damage()` | ダメージ処理完了後 |
| `magic_on_enemy_survive` | バトル結果確定直後 | `BattleSystem._apply_post_battle_effects()` | 再生スキル処理の後 |
| `legacy_magic` | 死亡時 | `BattleSpecialEffects.check_on_death_effects()` | 撃破判定後 |

---

## エラーハンドリング

### 1. PlayerSystem参照エラー

**問題**: `get_player_by_id()`メソッドが存在しない

**解決**: `players[player_id]`で直接アクセス

```gdscript
# ❌ 間違い
var player = player_system_ref.get_player_by_id(player_id)

# ✅ 正しい
if player_id >= 0 and player_id < player_system_ref.players.size():
	var player = player_system_ref.players[player_id]
```

### 2. プロパティ名エラー

**問題**: `player.magic`というプロパティは存在しない

**解決**: `player.magic_power`を使用

```gdscript
# ❌ 間違い
player.magic += amount

# ✅ 正しい
player.magic_power += amount
```

### 3. Null参照エラー

**問題**: `spell_magic_ref`が`null`

**原因**: 初期化順序が間違っている

**解決**: `spell_magic`取得を先に行う（上記「システム統合」参照）

---

## BattlePreparationでのスキップ処理

バトル準備時に以下の効果タイプはスキップする（特定タイミングで処理されるため）:

```gdscript
# battle_preparation.gd - apply_item_effects()
match effect_type:
	"legacy_magic":
		# 遺産効果は死亡時に処理されるため、ここでは何もしない
		pass
	
	"magic_from_damage":
		# 魔力獲得（ダメージ）効果はダメージ受け取り時に処理されるため、ここでは何もしない
		pass
	
	"magic_on_enemy_survive":
		# 魔力獲得（敵非破壊）効果はバトル結果確定後に処理されるため、ここでは何もしない
		pass
```

---

## 今後の拡張

### 追加効果候補

```gdscript
# パーセンテージベースの魔力操作
"magic_percent_increase": {
	"effect_type": "magic_percent_increase",
	"percentage": 10  # 現在魔力の10%増加
}

# 条件付き魔力獲得
"magic_on_condition": {
	"effect_type": "magic_on_condition",
	"condition": "enemy_element",
	"elements": ["fire"],
	"amount": 300
}

# 土地数に応じた魔力獲得
"magic_from_land_count": {
	"effect_type": "magic_from_land_count",
	"multiplier": 50,  # 所有土地数 × 50G
	"elements": ["fire", "earth"]  # 特定属性のみ
}
```

---

## デバッグログ

魔力増減時には以下のログが出力される:

```
[魔力増加] プレイヤー1 +200G → 合計:3200G
[魔力減少] プレイヤー2 -100G → 合計:2700G
[魔力奪取] プレイヤー2 -300G → プレイヤー1 +300G
【魔力獲得(ダメージ)】フェイのゼラチンアーマー → プレイヤー1が150G獲得（ダメージ30×5）
【魔力獲得(敵非破壊)】グラディエーターのゴールドハンマー → プレイヤー1が200G獲得
【遺産発動】アモンのゴールドグース → プレイヤー1が210G獲得（MHP30×7）
```

---

## 関連ドキュメント

- [カードドロー効果](./カードドロー.md) - SpellDrawシステム
- [スペルシステム設計](../spells_design.md) - 全体設計
- [アイテムシステム](../../implementation/item_system.md) - アイテム実装パターン

---

**最終更新**: 2025年11月3日（v1.0）
